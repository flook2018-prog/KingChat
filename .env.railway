# Railway Environment Variables for KingChat
# ⚠️ IMPORTANT: Set these in Railway Dashboard -> Variables

# Server Configuration
NODE_ENV=production
# PORT is automatically set by Railway (usually 8080)

# 🔐 Security (REQUIRED)
JWT_SECRET=your-super-secret-jwt-key-make-it-at-least-32-characters-long-and-random

# 🍃 Database (REQUIRED - Use MongoDB Atlas)
# Railway provides PostgreSQL but this app needs MongoDB
# Create MongoDB Atlas account and set this:
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/kingchat?retryWrites=true&w=majority

# 🌐 CORS (Railway Domain)
CORS_ORIGIN=https://kingchat.up.railway.app
FRONTEND_URL=https://kingchat.up.railway.app

# 🚂 Railway Environment Detection
RAILWAY_ENVIRONMENT=production

# 📱 LINE Bot (Optional - if using LINE integration)
# LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
# LINE_CHANNEL_SECRET=your-line-channel-secret

# ❌ Don't set these on Railway:
# NGROK_AUTH_TOKEN (not needed in production)
# DATABASE_URL (Railway PostgreSQL - not compatible)